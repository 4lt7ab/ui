# Theming

Placeholder — the theming concept doc is authored in a later task. Covers
tokens (primitives → semantic → components), themes, `useTheme`, the
`ThemePicker` component, and `usePageBackground`.
