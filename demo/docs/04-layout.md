# Layout

Placeholder — the layout concept doc is authored in a later task. Covers
`Stack`, `Container`, `Grid`, `Divider`, `Surface`, `Card`, and the
page-envelope organisms (`AppShell`, `DataTablePage`, `DetailPage`,
`EmptyPage`).
