# Getting started

Welcome to `@4lt7ab/ui`. This is a placeholder page — the full content
lands in the `core / content / motion` concept-docs task.

The scaffold proves the loading pipeline end-to-end: this markdown file was
glob-imported with `?raw`, parsed for title and order, and handed to
`@4lt7ab/content`'s `Markdown` component for rendering. The theme switcher
in the top-right cycles through the library's built-in themes — the prose
below picks up the active theme's tokens without reloading the page.

## What lives here once it's real

- Install instructions (git dependency with subpath exports)
- `ThemeProvider` setup
- First render
- Where to go next
